# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env

# Node
node_modules/
.next/
out/

# IDE
.vscode/
.idea/

# OS
.DS_Store

# Project
*.db
~/Transcriptions/
session-history/

# Local-only components (system monitoring widget)
# NOTE: frontend/src/components/SystemMonitor.tsx is intentionally NOT ignored —
# webpack resolves the wrapper's dynamic import at build time, so the module must
# exist in a fresh clone. A no-op stub is committed; override it locally with
# `git update-index --skip-worktree` if you have a real monitor.
backend/api/system.py
frontend/.env.local

# Local logs
logs/
